{"description":"When constraint is learned via field initializer (for final fields), flow message will be located in the method and not at the initializer line.\r\n\r\n{code}\r\nclass FieldReporting {\r\n  private final Object field \u003d new Object(); // flow@field {{Implies \u0027field\u0027 is non-null.}}  \u003c-- should be here \r\n  void f() {\r\n    if (field !\u003d null) { // Noncompliant [[flows\u003dfield]] flow@field {{Condition is always true.}} \"implies\" message is here\r\n\r\n    }\r\n  }\r\n}\r\n{code}","component":"SonarJava","component_version":"4.10"}