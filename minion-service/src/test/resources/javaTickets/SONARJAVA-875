{"description":"When feeding SonarQube symbol table some references may have a different length than their definition symbol (this is the case for enum constant). \n\nOnce the sonarqube API is able to receive this information, the java plugin should be updated to feed this information properly to the platform.\n\n{code}\npublic MyEnum {\n  FOO(\"foo\"),\n  BAR(\"bar\");\n\n  private MyEnum(String s) {\n  }\n{code}","component":"SonarJava","component_version":""}