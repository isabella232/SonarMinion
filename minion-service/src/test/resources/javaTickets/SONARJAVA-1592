{"description":"The current implementation target any class named {{*Error}} or {{*Exception}} while it should rely entirely on semantic  and target subclass of Exception/Error. Consequently, the current implementation generates numerous false positives on unrelated classes (POJO) with naming matching...\r\n\r\n{code}\r\nclass FooError {\r\n  int foo; // Noncompliant  \u003c--- should be compliant!\r\n}\r\n\r\nclass Foo extends Error {\r\n  int foo; // compliant  \u003c--- should be non-compliant!\r\n}\r\n{code}","component":"SonarJava","component_version":"3.13"}