{"description":"The rule should not raise issue when using class name of {{StackTraceElement}}, by calling to {{StackTraceElement.getClassName()}}, as it can not be replace by usage of {{instanceof}}.\r\n\r\n{code}\r\nStackTraceElement element \u003d getElement();\r\nif (MyClass.class.getName().equals(element.getClassName())) { // should be compliant\r\n}\r\n{code}","component":"SonarJava","component_version":"3.11"}