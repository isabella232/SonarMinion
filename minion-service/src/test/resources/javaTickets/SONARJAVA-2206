{"description":"The annotations on variable of for-each statements are currently not inserted into the syntax tree, and therefore can not be explored in rules.\r\n\r\n{code}\r\nvoid foo(List\u003cString\u003e myList) {\r\n  for(@Nullable Object object : myList) { // annotation can never be reach by scan.\r\n    // do something\r\n  }\r\n}\r\n{code}","component":"SonarJava","component_version":"4.8"}