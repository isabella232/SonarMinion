{"description":"In the following code, we wrongly resolve the method invocation, as we consider that it is a loose invocation, while a strict invocation was matching.\r\n\r\n{code}\r\nclass A {\r\n\r\n  public void foo(String a, Object... array) {\r\n    foo(a, false, array); // wrongly resolved to foo(String, Object[]) instead of foo(String,boolean,Object[])\r\n  }\r\n\r\n  private String foo(String a, boolean b, Object... array) { // FP unused private method\r\n    return a + b + array;\r\n  }\r\n}\r\n{code}","component":"SonarJava","component_version":"4.12"}