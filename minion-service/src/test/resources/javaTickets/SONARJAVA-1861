{"description":"{code}\r\nimport java.util.stream.Stream;\r\nimport java.util.stream.Collectors;\r\nimport java.util.List;\r\nclass A {\r\n\r\n  private void foo(List\u003cString\u003e l) {} // raise an unused private method because type of expression in call is solved to List\u003cT\u003e with T type var Collectors.toList\r\n\r\n  void test() {\r\n    List\u003cString\u003e l;\r\n    foo(l.stream().sorted().collect(Collectors.toList())); \r\n  }\r\n\r\n}\r\n{code}","component":"SonarJava","component_version":"4.2.1"}