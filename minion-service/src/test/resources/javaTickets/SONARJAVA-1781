{"description":"As we are not able to list types from a package when reading on-demand imports, we are not able to tell if the fully qualified name should be removed. The current implementation of RSPEC-1942 currently ignores all files with at least one star-import.\r\n\r\n{code}\r\nimport java.util.*;\r\nimport java.awt.*;\r\n\r\nclass A {\r\n  java.util.List myList1; // As there is a List class in both package, we can not remove the fully qualified names because of ambiguities\r\n  java.awt.List myList2; \r\n}\r\n{code}","component":"SonarJava","component_version":""}