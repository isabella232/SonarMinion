{"description":"Default methods from interface are non-abstract methods in bytecode. The {{default}} modifier is not a flag from the jvm.\r\n\r\nWhen parsing interfaces, we manually add a {{default}} flag to all the methods encountered having the {{default}} modifier. We should consequently flag methods the same way when reading from bytecode.","component":"SonarJava","component_version":"4.7"}