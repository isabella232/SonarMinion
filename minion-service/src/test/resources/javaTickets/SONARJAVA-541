{"description":"This rule only check for {{printStackTrace}} calls, so the following code raise an issue : \n{code}\ntry {\n//â¦\n} catch (MyException ex) {\norg.openide.util.Exceptions.printStackTrace(ex);\n}\n{code}\n\nCheck should be improved so that only printStackTrace on a class that inherits from Exception or Throwable are raising exceptions.","component":"SonarJava","component_version":"2.4"}