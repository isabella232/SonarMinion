{"description":"Text range in which issue are reported has been enforced in order to not be empty. \r\nWith the current implementation of the rule, we report on the last line of the file, with an empty range.\r\n\r\nThe check should be modified in order to raise an issue only at file level.\r\nA check should also enforce the fact that we should never report issue on empty ranges.\r\n\r\nWhen the range calculated based on the provided trees is empty, 3 approaches are possible:\r\n- report on the full line\r\n- report on the file\r\n- fail on badly on our side (preconditions)","component":"SonarJava","component_version":"4.4"}