{"description":"The {{TypeTree}} associated to the variable of lambda expression usually are {{InferedTypeTree}}, which can not be used to report issues. In such cases, the issue should be reported on the variable directly.\r\n\r\n{code}\r\nclass UseLambda {\r\n  void foo(Vector\u003cVector\u003cString\u003e\u003e v) {\r\n    Collections.sort(v, (s1, s2) -\u003e -1); // the check try to add an issue on the type trees of s1 and s2, which do not exist\r\n  }\r\n}\r\n{code}","component":"SonarJava","component_version":"4.2.1"}