{"description":"continue statement in For each loops are badly handled in CFG resulting in failure during symbolic execution : \r\n\r\n{code}\r\n  public void continue_foreach(boolean a, boolean b, Map\u003cString, String\u003e map) {\r\n    for (String prop : map.keySet()) {\r\n      if (b) {\r\n        continue;\r\n      }\r\n      String.format(\"  - %s\u003d%s\", prop, a ? \"******\" : \"\");\r\n    }\r\n  }\r\n{code}","component":"SonarJava","component_version":"3.8"}