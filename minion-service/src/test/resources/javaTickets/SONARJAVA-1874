{"description":"{code}\r\nprivate static List\u003cSet\u003cType\u003e\u003e erased(List\u003cSet\u003cType\u003e\u003e typeSets) {\r\n    return typeSets.stream().map(set -\u003e set.stream().map(Type::erasure)\r\n      .collect(Collectors.toCollection(LinkedHashSet::new))) // type of LinkedHashSet::new is not properly infered resulting in unknown method lookup afterwards\r\n      .collect(Collectors.toList());\r\n  }\r\n{code}","component":"SonarJava","component_version":"4.2.1"}