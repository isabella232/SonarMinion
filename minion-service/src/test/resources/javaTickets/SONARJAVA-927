{"description":"Annotations on enum constants are not available from the syntax tree : they are parsed properly and passed to the method in TreeFactory that is in charge of constructing the node, but modifiers of the enum constant nodes is always empty : \nhttps://github.com/SonarSource/sonar-java/blob/master/java-squid/src/main/java/org/sonar/java/ast/parser/TreeFactory.java#L497\n\n{code}\nEnumConstantTreeImpl result \u003d new EnumConstantTreeImpl(ModifiersTreeImpl.EMPTY, identifier, newClass);\n{code}\n\nThis will raise FP on unusued import rule :\n{code}\nimport my.Annotation; //will raise unused import FP.\nenum foo {\n@Annotation BAR; //Annotation is not analyzed by rule as it is not present in Syntax Tree\n}\n{code}","component":"SonarJava","component_version":"3.1"}