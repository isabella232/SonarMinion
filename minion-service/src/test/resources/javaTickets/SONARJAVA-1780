{"description":"Any null check  on a symbolic value already having a status makes it lose the known status:\r\n\r\nExample:\r\n{code}\r\nvoid foo(File file) {\r\n  FileInputStream fis \u003d new FileInputStream(file);   // SV(fis) is NOT_NMULL, with status OPEN\r\n  if (fis !\u003d null) {\r\n    // within on true branch, SV(fis) is now NOT_NULL only,  and the OPEN status is lost.\r\n  }\r\n}\r\n{code}","component":"SonarJava","component_version":"4.1"}