{"description":"Currently this check relies only on identifier to trigger an issue whereas it should relies on symbol table to raise issue only on correct types. \n\n{code}\nclass A {\n  class Stack {}\n  void foo(Stack stack) { //false positive\n  }\n}\n{code}","component":"SonarJava","component_version":"2.7"}