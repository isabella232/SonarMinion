{"description":"Null and boolean literals are returning the same Symbolic values when evaluated by the SE engine.\r\n\r\nThis is an issue when reporting. The two following piece of codes should have a reporting which is different but as null literal SV is the same used everywhere : it is filtered out as we are not able to determine which expression generated it and how it flowed through the program. \r\n{code}\r\nObject foo \u003d null;                   Object foo \u003d null;\r\nObject bar \u003d null;                   Object bar \u003d foo;\r\nif(bar \u003d\u003d null) { ... }             if(bar \u003d\u003d null) { ... }\r\n{code}","component":"SonarJava","component_version":""}