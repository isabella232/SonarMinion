{"description":"{code}\r\n    try (Stream foo3 \u003d new Stream()) {\r\n      foo3.bar();\r\n    }\r\n    try (Stream foo3 \u003d new Stream()) {\r\n      foo3.bar();\r\n    }\r\n{code}\r\n\r\nThis code is semantically valid but we resolve it wrongly by consdiering only one of the two variables will be used because we put those variables in the scope of the owning method instead of creating their own.","component":"SonarJava","component_version":"3.11"}