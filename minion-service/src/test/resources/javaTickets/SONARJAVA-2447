{"description":"Because loop bodies are visited twice during symbolic execution, null dereference issue inside the loop body will have duplicated flow reported, once when NPE happens during first past , and second time when NPE happens on second pass. Also, one flow will be subset of the other.\r\n\r\nOnly one flow should be reported in such cases.\r\n\r\n{code}\r\npublic class FlowStutter {\r\n\r\n  private String possiblyNull() {\r\n    String opt \u003d null;\r\n    if (cond) {\r\n      opt \u003d \"\";\r\n    }\r\n    return opt;\r\n  }\r\n\r\n  void test() {\r\n    while (condition) {\r\n      final String val \u003d possiblyNull();\r\n      val.length();\r\n    }\r\n  }\r\n}\r\n{code}","component":"SonarJava","component_version":""}