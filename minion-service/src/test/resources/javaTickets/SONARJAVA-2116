{"description":"{code}\r\nclass A {\r\n  void fp(int param) {\r\n    if(xproc(param) \u003e param) { // False positive on condition always true or false\r\n      System.out.println(\"\");\r\n    }\r\n    System.out.println(\"\");\r\n  }\r\n\r\n  private int xproc(int param) {\r\n    if(unknownCondition) {\r\n      return -1;\r\n    }\r\n    return param;\r\n  }\r\n}\r\n{code}\r\n\r\nThe FP in the example comes from the fact that two yields will be chosen from the xproc method but their difference will only lies in the stack and only the top of the stack is looked up by the program state equality.","component":"SonarJava","component_version":"4.6"}