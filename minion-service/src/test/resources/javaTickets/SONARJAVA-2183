{"description":"When calling a varargs method with no argument :\r\n{code}\r\nprivate class VarArgsWithNoArg {\r\n  private String[] directories(String... values) {\r\n    return values;\r\n  }\r\n\r\n  String[] test() {\r\n    return directories(); // problematic call\r\n  }\r\n}\r\n{code}\r\n\r\nThe xprocedural component of the symbolic execution engine will try to apply some constraint to the argument resulting in an index out of bounds exception failing the analysis.","component":"SonarJava","component_version":"4.7"}