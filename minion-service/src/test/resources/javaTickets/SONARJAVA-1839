{"description":"In the following example, the type {{this::bar}} is resolved to {{ThreadGroup}}, which is not possible as parameters of the lambda are not compatible.\r\n\r\n{code}\r\nclass Foo {\r\n\r\n  void foo() {\r\n    new Thread(this::bar1, \"name\"); // bar1 not resolved\r\n    new Thread(this::bar2); // bar2 resolved\r\n  }\r\n\r\n  private void bar1() {\r\n    // do nothing\r\n  }\r\n\r\n  private void bar2() {\r\n    // do nothing\r\n  }\r\n\r\n}\r\n{code}","component":"SonarJava","component_version":"4.2.1"}